This is a prototype of polling module  that is updated daily.

polling_icon_dependencies  are files that suppose to be put on to the directories (replace old files)
so polling icon could be available.  
